/* Log Level Constants
   Defines the standardized log level strings used throughout the application.
   These constants ensure consistency in logging across all services. */
module.exports = {
    INFO:'INFO',      // Informational messages about normal application operation
    ERROR:'ERROR',    // Error conditions that need attention
    WARN:'WARN',      // Warning messages for potentially problematic situations
    FATAL:'FATAL',    // Critical errors that may cause application failure
};